export enum FunctionName {
    Deposit = "deposit",
    Approve = "approve",
    Allowance = "allowance",
    Decimals = "decimals",
    Balance = "balanceOf"
}

export interface GameType {
    type: string;
    variant: string;
}