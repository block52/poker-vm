# Block52 LOCAL Testnet Faucet Configuration
# Use this file for local development: cp .env.local .env

# Bob's account (from config.yml genesis) - has ~899,698 STAKE locally
# Address: b521hg93rsm2f5v3zlepf20ru88uweajt3nf492s2p
FAUCET_MNEMONIC="vanish legend pelican blush control spike useful usage into any remove wear flee short october naive swear wall spy cup sort avoid agent credit"

# Server port
PORT=3001

# Network endpoints (LOCAL testnet)
RPC_ENDPOINT=http://localhost:26657
REST_ENDPOINT=http://localhost:1317
CHAIN_ID=pokerchain
